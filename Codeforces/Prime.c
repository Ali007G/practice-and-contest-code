#include<stdio.h>
#include<stdlib.h>
int main()
{
    int i,n,count=0,a;
    printf("Enter number: ");
    scanf("%d", &n);

    for(i=2;i<=n/2;i++)
    {
        scanf("%d", &a);
        if(n%i==1)
        count++;
        {
        printf("%d", n);
    }
}}
